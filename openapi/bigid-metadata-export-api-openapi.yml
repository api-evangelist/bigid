openapi: 3.2.0
info:
  title: BigID Data Catalog Metadata Export API
  description: Query and export BigID's data catalog — the central inventory of objects, columns, and attributes discovered by BigID scans. Includes endpoints for metadata export, duplicate-data analysis, and column-level cluster similarity lookups.
  version: '1.0'
  contact:
    name: BigID Support
    url: https://developer.bigid.com/
    email: support@bigid.com
  license:
    name: BigID Terms of Service
    url: https://bigid.com/terms/
servers:
- url: https://{deployment}.bigid.com/api/v1
  description: Customer-hosted BigID deployment.
  variables:
    deployment:
      default: tenant
tags:
- name: Metadata Export
  description: Export catalog metadata.
paths:
  /data-catalog/object-details/columns:
    get:
      tags:
      - Metadata Export
      operationId: getCatalogObjectColumns
      summary: Get Catalog Object Columns
      description: Retrieve column-level metadata for catalog objects.
      security:
      - BearerAuth: []
      responses:
        '200':
          description: Column metadata retrieved.
          content:
            application/json:
              schema:
                type: object
  /data-catalog/object-details/attributes:
    get:
      tags:
      - Metadata Export
      operationId: getCatalogObjectAttributes
      summary: Get Catalog Object Attributes
      description: Retrieve attribute-level metadata (classifier hits) for unstructured catalog objects.
      security:
      - BearerAuth: []
      responses:
        '200':
          description: Attributes retrieved.
          content:
            application/json:
              schema:
                type: object
components:
  securitySchemes:
    BearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
